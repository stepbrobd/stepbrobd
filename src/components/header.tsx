import {FunctionComponent} from "react";

const Header: FunctionComponent = () => {
    return (
        <header className="sticky top-0 z-10 backdrop-filter backdrop-blur-lg bg-opacity-30">

        </header>
    )
}

export default Header
